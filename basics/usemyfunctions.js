//const add = require('./myfunctions').addfun; //No need to import js file
//const mul = require('./myfunctions').mulfun; 

const add = require('./myfunctions').addx; //No need to import js file
const mul = require('./myfunctions').mulx; 

console.log(add(10,20));
console.log(mul(10,20));